import {
  HeartPulse,
  Activity,
  PersonStanding,
  Stethoscope,
  type LucideIcon,
} from "lucide-react";

// Map of icon names referenced in content (specialites.ts) to lucide components.
export const iconMap: Record<string, LucideIcon> = {
  HeartPulse,
  Activity,
  PersonStanding,
  Stethoscope,
};
